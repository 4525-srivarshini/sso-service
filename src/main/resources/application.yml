spring:
  server:
    port: 8080
  banner:
    location: /static/banner.txt
  datasource:
    url: jdbc:mysql://localhost:3306/EduBox
    username: root
    password: Varshini@29
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: saisantosh1492@gmail.com
    password: eixo saad papd mjga
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
